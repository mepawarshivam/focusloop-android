<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Backend (Firebase Auth + Firestore) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Usage stats for detecting foreground app -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools"/>

    <!-- Overlay for intervention UI -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Boot receiver to restart monitoring service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Vibration for interventions -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Package visibility (Android 11+): needed to look up real icons for the
         known distracting apps during onboarding, since PACKAGE_USAGE_STATS
         alone doesn't grant getApplicationIcon() visibility into other apps. -->
    <queries>
        <package android:name="com.zhiliaoapp.musically"/>
        <package android:name="com.instagram.android"/>
        <package android:name="com.google.android.youtube"/>
        <package android:name="com.reddit.frontpage"/>
        <package android:name="com.twitter.android"/>
        <package android:name="com.facebook.katana"/>
        <package android:name="com.snapchat.android"/>
        <package android:name="com.android.chrome"/>
        <package android:name="com.linkedin.android"/>
        <package android:name="com.pinterest"/>
    </queries>

    <application
        android:name=".FocusLoopApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusLoop">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Intervention overlay activity — shown over other apps -->
        <activity
            android:name=".ui.intervention.InterventionActivity"
            android:exported="false"
            android:theme="@style/Theme.FocusLoop.Transparent"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"/>

        <!-- Focus monitoring foreground service -->
        <service
            android:name=".service.FocusMonitoringService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_monitoring"/>
        </service>

        <!-- Boot receiver — restarts monitoring if it was active before reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
